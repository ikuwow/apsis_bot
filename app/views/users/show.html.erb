<% provide(:title, @user.name) %>

<h1>
    <%= gravatar_for @user %>
    <%= @user.name %>
</h1>

<p>Email: <%= @user.email %></p>

<% if logged_in? %>
  <p>
  <%= link_to "Log out",
      { :controller => 'sessions', :action => 'destroy' },
      { :method => 'delete' }
  %>
  </p>
<% end %>

